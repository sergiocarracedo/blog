---
import { locales, defaultLocale, type Locale } from '@/i18n';

export function getStaticPaths() {
  return locales
    .filter((l) => l !== defaultLocale)
    .map((l) => ({ params: { lang: l } }));
}

const { lang } = Astro.params;
return Astro.rewrite(`/${lang}/blog/page/1`);
---
