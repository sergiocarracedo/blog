---
import { locales, defaultLocale } from '@/i18n';

export function getStaticPaths() {
  return locales
    .filter((l) => l !== defaultLocale)
    .map((l) => ({ params: { lang: l } }));
}

return Astro.rewrite('/about');
---
